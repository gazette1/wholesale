{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window. The window only loads the hosted app, so it needs the updater and nothing else.",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
