# Acquisitions CRM with Deal Analyzer

Internal system for a residential wholesale and flip operation: seller lead pipeline, property reports, SMS and email follow up, a deal analyzer ported from the team's spreadsheet, an investor buyer network, and investor facing deal packages.

## What is in the repo

| Path | Contents |
|---|---|
| `apps/web` | Next.js 15 app. Every screen, server action, webhook, and the PDF renderer. No math outside the engine. |
| `packages/engine` | Pure TypeScript calculators ported from the workbook with golden tests. Runs in the browser and on the server. |
| `packages/db` | Drizzle schema, SQL migrations, seed data, PGlite fallback for local work. |
| `packages/integrations` | Provider boundaries with real and mock adapters: RealEstateAPI, Twilio, Resend, TypeSafe Jev, file storage. |
| `spec/` | Workbook extraction, formula spec, anomalies, golden fixtures. Never hand edited. |
| `docs/` | Architecture, decisions, CRM proposal, integrations guide, implementation plan, and `MAC_PARITY.md` (what came over from the Mac app, what did not, and why). |
| `tools/` | `extract_workbook.py`, rerun when a new workbook arrives. |

## Deal analyzer at a glance

Every analysis belongs to a property and shows on its pipeline card, its quick view drawer, and its lead. Input sections: Deal, Offers (comparables, per foot tiers, seller value), Rehab (checklist, manual, or per square foot, with work status and a progress log), Financing, Holding, Closing costs, Buy and hold, Loan. Result tabs: Offers, Flip P&L, Cash flow, Rehab, Buy and hold, Loan, Sensitivity, Compare. A validation summary names what needs fixing and links to the section. The library on `/analyzer` has search, filters, sort, archive, trash, restore, and duplicate. Versions lock when approved; clone to edit.

## Run it locally

Requirements: Node 20 or newer, pnpm 10 (`npm i -g pnpm`). Python 3.11 only for the workbook extractor.

```bash
pnpm install
pnpm test
pnpm dev
```

Open http://localhost:3000. With no `DATABASE_URL` the app creates a file backed PGlite database under `.pglite/`, migrates it, and loads demo data (40 leads, 12 buyers, analyses, campaigns) on first request. With `DEV_AUTH_EMAIL` set to a seeded profile email you are signed in without Supabase.

## Environment variables

Copy `.env.example` to `apps/web/.env.local`. Next.js reads only that location. Every provider falls back to a mock adapter when its keys are missing, so the app runs end to end with an empty file.

| Variable | Purpose |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase Auth and storage. Public. |
| `SUPABASE_SERVICE_ROLE_KEY` | Server side storage uploads. Secret. |
| `DATABASE_URL` | Postgres connection string (Supabase pooler, port 6543). Without it the app uses PGlite. |
| `DEV_AUTH_EMAIL` | Development only. Signs you in as the profile with this email. Remove before deploying. |
| `DEMO_MODE` | `true` lets `DEV_AUTH_EMAIL` work in production for a public demo link on sample data. Never set on a real deployment. |
| `PROPERTY_DATA_PROVIDER`, `REALESTATEAPI_KEY` | `realestateapi` plus a key enables live property reports and comps. |
| `MESSAGING_PROVIDER`, `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_FROM_NUMBER` or `TWILIO_MESSAGING_SERVICE_SID` | `twilio` enables live SMS. |
| `EMAIL_PROVIDER`, `RESEND_API_KEY`, `EMAIL_FROM` | `resend` enables live email. |
| `JUDGMENT_PROVIDER`, `TYPESAFE_API_KEY` | `typesafe` enables Jev for reply classification, deal issue suggestions, and buyer criteria. |
| `APP_URL` | Public URL, used for webhook callbacks and share links. |
| `CRON_SECRET` | Bearer token the scheduler sends to `/api/cron/dispatch`. |

## Database

Migrations live in `packages/db/migrations` and are applied to Supabase through the Supabase MCP or the SQL editor. `0000_crm_init.sql` is generated by drizzle-kit from `packages/db/src/schema`; `0001_rls_and_triggers.sql` is hand written (row level security, triggers, storage bucket).

To change the schema: edit `packages/db/src/schema/*.ts`, run `pnpm --filter @dealcalc/db generate`, review the new SQL file, apply it. The generate script also refreshes `packages/db/src/migrations.generated.ts`, an embedded copy of the SQL that the PGlite fallback uses at runtime, so serverless bundles need no migrations folder. A test fails if the copy drifts from the files on disk.

To seed the hosted database once `DATABASE_URL` is set:

```bash
DATABASE_URL=postgresql://... pnpm --filter @dealcalc/db seed
```

## Webhooks

| URL | Provider setting |
|---|---|
| `POST /api/webhooks/twilio/sms` | Phone number, "A message comes in" |
| `POST /api/webhooks/twilio/status` | Set automatically as StatusCallback when `APP_URL` is set |
| `POST /api/webhooks/resend` | Resend dashboard, webhook for email events; set `RESEND_WEBHOOK_SECRET` |
| `GET /api/cron/dispatch` | Scheduler, every minute, `Authorization: Bearer $CRON_SECRET`. `apps/web/vercel.json` configures Vercel Cron. |

Other software (Zapier, Make, web forms, dialers, spreadsheets) connects through the inbound REST API at `/api/v1`, signed outbound webhooks, and CSV import and export, all managed under Settings, Integrations. See "Connecting other software" in `docs/INTEGRATIONS.md`.

## Deploy

1. Create a Vercel project from `apps/web` with root directory `apps/web` and framework Next.js. Install command `pnpm install`, build command `pnpm build`.
2. Set every variable from the table above. Remove `DEV_AUTH_EMAIL`.
3. Supabase: Authentication, URL configuration: site URL is the Vercel URL and `https://<app>/login/callback` is an allowed redirect.
4. Point Twilio and Resend webhooks at the Vercel URL.
5. Team members sign up at `/login?mode=signup` with the email an admin added under Settings, Team.

Desktop installers for Mac and Windows come from wrapping the deployed URL with Tauri (Phase 2 of `docs/IMPLEMENTATION_PLAN.md`).

## Tests

```bash
pnpm test
```

Engine: 64 tests. The golden and edge tests pin every workbook calculator to the spreadsheet's cached values; the rest cover the additions that are not in the workbook (quick offer tiers, comparable average, seller value score, rehab source, loan schedule, validation, the deal runner). Database: schema, seed, and the migration ledger on PGlite. Integrations: provider mapping, signature verification, judgment parsing, CSV, webhook signing, URL policy, lead payload parsing.

## Writing rules for documents in this repo

No em dashes, no exclamation points, plain sentences. Thousands as M and millions as MM in financial documents.
